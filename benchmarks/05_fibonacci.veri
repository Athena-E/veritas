// NOTE: Loops are currently broken - this is a placeholder
// Testing nested function calls instead
fn double(x: int) -> int {
    x + x
}

fn quad(x: int) -> int {
    double(double(x))
}

fn octo(x: int) -> int {
    double(quad(x))
}

fn main() -> int {
    octo(5)
}
