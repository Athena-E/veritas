fn return_add(x: int, y: int) -> int {
    let res: int = x + y;
    return res;
}

fn implicit_add(x: int, y: int) -> int {
    x + y
}

fn compute(x: int) -> int {
    let y: int = x * 2;
    y + 10
}

fn print_value(x: int) {
    let temp: int = x;
}