
fn safe_access(index: int) requires index >= 0 {
    let arr: [int; 10] = [0; 10];
}

fn safe_divide(dividend: int, divisor: int) requires divisor != 0 {
}

fn test_safe_call() {
    safe_access(5);
    safe_divide(10, 2);
}

fn test_refined_call() {
    let positive: int = 42;
    safe_access(positive);
}
