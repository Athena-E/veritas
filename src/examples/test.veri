fn read(r: &int) {
    let x: int = *r;
}

fn write(r: &mut int) -> int {
    *r = 9;
    *r
}

fn main() -> int {
    let mut x: int = 0;
    write(&mut x);
    x
    //let mx: &mut int = &mut x;
    //x = 2;
}