fn main() -> int {
    let mut a: [[int; 96]; 96] = [[0; 96]; 96];
    let mut b: [[int; 96]; 96] = [[0; 96]; 96];
    let mut x: [int; 96] = [0; 96];
    let mut y: [int; 96] = [0; 96];

    for i in 0..96 {
        x[i] = (i + 1) % 11;
        for j in 0..96 {
            a[i][j] = (i * (j + 1) + 1) % 13;
            b[i][j] = (i * (j + 2) + 2) % 17;
        }
    }

    for i in 0..96 {
        let mut tmp1: int = 0;
        let mut tmp2: int = 0;
        for j in 0..96 {
            tmp1 = tmp1 + a[i][j] * x[j];
            tmp2 = tmp2 + b[i][j] * x[j];
        }
        y[i] = 3 * tmp1 + 2 * tmp2;
    }

    let mut sum: int = 0;
    for i in 0..96 {
        sum = sum + y[i];
    }

    print_int(sum);
    print_char(10);
    sum
}
